import { iLibHeader } from '../model/library-header.models';

export const LIBRARY_HEADER_CONFIG: iLibHeader = {
  title: 'storybook.header.title',
  subtitle: 'storybook.header.subtitle',
} as const;
