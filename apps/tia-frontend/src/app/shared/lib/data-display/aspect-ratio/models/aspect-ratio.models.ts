export type AspectRatioTone = 'soft' | 'light' | 'accent';

export interface AspectRatioItem {
  id: string;
  label: string;
  description: string;
  ratio: string;
  width: string;
  background: string;
}
